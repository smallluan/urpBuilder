import React from 'react';

const FlowBody: React.FC = () => {
  return (
    <div className="flow-body">
      <h2>搭建流程</h2>
      <p>这是搭建流程的主区域，布局与搭建组件一致，但内容不同。</p>
    </div>
  );
};

export default FlowBody;
